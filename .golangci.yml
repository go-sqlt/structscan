version: "2"

run:
  tests: false

linters:
  default: all
  disable:
    - wrapcheck
    - varnamelen
    - revive
    - ireturn
    - err113
    - gochecknoglobals

  exclusions:
    paths-except:
      - main.go
